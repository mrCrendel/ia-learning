[project]
name = "ai-learning"
version = "0.1.0"
description = "Учебный репозиторий курса AI-инженерии"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "openai",
    "pydantic",
    "httpx",
    "python-dotenv",
    "pytest",
]
